# Cloudflare Worker config for the recognition CORS relay.
# Deploy:  cd proxy && npx wrangler login && npx wrangler deploy
name = "szm-prx"
main = "recognition-worker.js"
compatibility_date = "2024-11-01"
